apiVersion: v1
kind: Service
metadata:
  name: monitor-service
  namespace: default
spec:
  selector:
    app: monitor-service
  ports:
    - protocol: TCP
      port: 5002
      targetPort: 5002
  type: ClusterIP